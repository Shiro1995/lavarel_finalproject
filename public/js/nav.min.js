/**
 * Created by vuongluis on 10/13/2019.
 */
$(document).ready(function() {

    $("#nav_index").click (function (event) {
        event.preventDefault();
        $.ajax({
            url: 'admin/ajax/dashboard',
            headers: {
                'X-CSRF-TOKEN': $('meta[name="csrf-token"]').attr('content')
            },
            dataType: 'json',
            type: 'GET',
            beforeSend: function() {
                $('#modal-loading').modal('show');
            }
        })
            .done(function(data) {
                $('#modal-loading').modal('hide');
                $('#page_content_ajax').replaceWith(data['html']);
            });
    });

    $("#nav_view_category").click (function (event) {
        event.preventDefault();
        $.ajax({
            url: 'admin/ajax/category',
            headers: {
                'X-CSRF-TOKEN': $('meta[name="csrf-token"]').attr('content')
            },
            dataType: 'json',
            type: 'GET',
            beforeSend: function() {
                console.log("Start Navigate to View Categorys");
                $('#modal-loading').modal('show');
            }
        })
            .done(function(data) {
                console.log(data['html']);
                $('#modal-loading').modal('hide');
                $('#page_content_ajax').replaceWith(data['html']);
            });
    });
    $("#nav_view_type_disease").click (function (event) {
        event.preventDefault();
        $.ajax({
            url: 'admin/ajax/type_disease',
            headers: {
                'X-CSRF-TOKEN': $('meta[name="csrf-token"]').attr('content')
            },
            dataType: 'json',
            type: 'GET',
            beforeSend: function() {
                $('#modal-loading').modal('show');
            }
        })
            .done(function(data) {
                $('#modal-loading').modal('hide');
                $('#page_content_ajax').replaceWith(data['html']);
            });
    });
    $("#nav_view_disease").click (function (event) {
        event.preventDefault();
        $.ajax({
            url: 'admin/ajax/disease',
            headers: {
                'X-CSRF-TOKEN': $('meta[name="csrf-token"]').attr('content')
            },
            dataType: 'json',
            type: 'GET',
            beforeSend: function() {
                $('#modal-loading').modal('show');
            }
        })
            .done(function(data) {
                $('#modal-loading').modal('hide');
                $('#page_content_ajax').replaceWith(data['html']);
            });
    });

    $("#nav_view_definitions").click (function (event) {
        event.preventDefault();
        $.ajax({
            url: 'admin/ajax/definitions',
            headers: {
                'X-CSRF-TOKEN': $('meta[name="csrf-token"]').attr('content')
            },
            dataType: 'json',
            type: 'GET',
            beforeSend: function() {
                $('#modal-loading').modal('show');
            }
        })
            .done(function(data) {
                $('#modal-loading').modal('hide');
                $('#page_content_ajax').replaceWith(data['html']);
            });
    });
    $("#nav_view_prognostics").click (function (event) {
        event.preventDefault();
        $.ajax({
            url: 'admin/ajax/prognostics',
            headers: {
                'X-CSRF-TOKEN': $('meta[name="csrf-token"]').attr('content')
            },
            dataType: 'json',
            type: 'GET',
            beforeSend: function() {
                $('#modal-loading').modal('show');
            }
        })
            .done(function(data) {
                $('#modal-loading').modal('hide');
                $('#page_content_ajax').replaceWith(data['html']);
            });
    });
    $("#nav_view_reasons").click (function (event) {
        event.preventDefault();
        $.ajax({
            url: 'admin/ajax/reasons',
            headers: {
                'X-CSRF-TOKEN': $('meta[name="csrf-token"]').attr('content')
            },
            dataType: 'json',
            type: 'GET',
            beforeSend: function() {
                $('#modal-loading').modal('show');
            }
        })
            .done(function(data) {
                $('#modal-loading').modal('hide');
                $('#page_content_ajax').replaceWith(data['html']);
            });
    });


});
